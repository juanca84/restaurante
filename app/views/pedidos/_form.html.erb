<%= form_with(model: pedido, local: true) do |form| %>
  <% if pedido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pedido.errors.count, "error") %> prohibited this pedido from being saved:</h2>

      <ul>
      <% pedido.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :numero %>
    <%= form.number_field :numero, id: :pedido_numero %>
  </div>

  <div class="field">
    <%= form.label :total %>
    <%= form.text_field :total, id: :pedido_total %>
  </div>

  <div class="field">
    <%= form.label :mesa_id %>
    <%= form.text_field :mesa_id, id: :pedido_mesa_id %>
  </div>

  <div class="field">
    <%= form.label :cliente %>
    <%= form.text_field :cliente, id: :pedido_cliente %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
