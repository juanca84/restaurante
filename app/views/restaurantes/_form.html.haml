= form_with(model: restaurante, local: true) do |form|
  - if restaurante.errors.any?
    #error_explanation
      %h2
        = pluralize(restaurante.errors.count, "error")
        prohibited this restaurante from being saved:
      %ul
        - restaurante.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :nit
    = form.text_field :nit, id: :restaurante_nit
  .field
    = form.label :nombre
    = form.text_field :nombre, id: :restaurante_nombre
  .actions
    = form.submit
